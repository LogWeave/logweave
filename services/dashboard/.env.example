# Dashboard configuration
VITE_LOGWEAVE_API_URL=
VITE_LOGWEAVE_API_KEY=dev-key

# Polling interval (milliseconds) — increase for less API load, decrease for live tail
VITE_POLL_INTERVAL_MS=60000

# =============================================
# TODO: API key is baked into the JS bundle.
# This is acceptable for self-hosted deployments.
# For SaaS: implement session-based auth with
# a /v1/auth/token endpoint that exchanges
# credentials for a short-lived JWT.
# =============================================
