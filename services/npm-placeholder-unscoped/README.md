# logweave

Placeholder for the unscoped `logweave` name on npm.

LogWeave is a log intelligence platform — pattern extraction, anomaly
detection, and a structured query surface for AI agents.

Real packages live under the `@logweave` scope. This unscoped name is
reserved to prevent confusion or namespace squatting. It does not export
anything and should not be installed.
