{
  "name": "logweave-simulator",
  "private": true,
  "type": "module",
  "description": "Log simulator — config-driven event generator for development + demo",
  "scripts": {
    "start": "tsx src/index.ts",
    "test": "node --import tsx --test tests/*.test.ts",
    "typecheck": "tsc --noEmit",
    "lint": "biome check ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.13",
    "@types/node": "^25.6.0",
    "tsx": "^4.21.0",
    "typescript": "catalog:",
    "winston": "^3.19.0",
    "winston-transport": "^4.9.0"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1040.0"
  }
}
